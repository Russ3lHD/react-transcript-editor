import React from 'react';

import App from '../app';

const meta = {
  title: 'Demo',
  component: App,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
};

export default meta;

export const Default = {
  args: {},
};
